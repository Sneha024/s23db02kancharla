//- extends layout

//- block content
  //- h1= title
  //- p Hai #{title}

  //- button(type="button", class="btn btn-success" onclick="goFaster()") Press Me
  //- div#faster Increase the speed 
  //- a(href="/book") Book Search Results
  //- script.
    //- let mph = 20;
    //- let change = 1;
    //- function goFaster() {
    //-   mph += change;
    //-   change += 1;
    //-   document.getElementById('faster').textContent = `MPH ${mph} with change ${change}`; 
    //- }

extends layout

block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/book") Take me to book
    br
    a(href="/logout") Logout
